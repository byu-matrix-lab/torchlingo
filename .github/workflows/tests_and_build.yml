name: Test, Build, and Publish Python package

on:
  push:
    branches: [main]
    paths:
      - 'src/**'
      - 'tests/**'
      - 'scripts/**'
      - 'pyproject.toml'
      - 'MANIFEST.in'
      - '.pre-commit-config.yaml'
      - '.github/workflows/tests_and_build.yml'
    tags: ['v*']
  pull_request:
    branches: [main]
    paths:
      - 'src/**'
      - 'tests/**'
      - 'scripts/**'
      - 'pyproject.toml'
      - 'MANIFEST.in'
      - '.pre-commit-config.yaml'
      - '.github/workflows/tests_and_build.yml'
  workflow_dispatch: {}

jobs:
  lint:
    name: Lint and format check
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      # Read the pinned ruff spec straight out of pyproject.toml so this
      # workflow cannot drift from the dev extra or from
      # .pre-commit-config.yaml. Ruff's default rule set widens across
      # releases, so a version mismatch changes what "clean" means.
      - name: Install pinned ruff
        run: |
          python -m pip install --upgrade pip
          RUFF_SPEC=$(python -c "
          import tomllib
          with open('pyproject.toml', 'rb') as f:
              dev = tomllib.load(f)['project']['optional-dependencies']['dev']
          print(next(d for d in dev if d.startswith('ruff')))
          ")
          echo "Installing $RUFF_SPEC"
          pip install "$RUFF_SPEC"

      - name: Ruff lint
        run: ruff check src tests

      - name: Ruff format check
        run: ruff format --check src tests

  tests:
    name: Run tests
    runs-on: ubuntu-latest
    permissions:
      contents: read
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13"]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: 'pip'
          cache-dependency-path: pyproject.toml

      - name: Install project with dev+asian extras
        run: |
          python -m pip install --upgrade pip
          pip install --extra-index-url https://download.pytorch.org/whl/cpu ".[dev,asian]"

      - name: Create SentencePiece test models
        run: |
          python scripts/generate_sentencepiece_models.py --example data/example.tsv --n 2000

      - name: Run test suite
        run: |
          python -m pytest tests/

  build:
    name: Build wheels and sdist
    needs: [tests, lint]
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: pyproject.toml

      - name: Install build tool
        run: |
          python -m pip install --upgrade pip
          pip install build

      - name: Build wheel
        run: |
          python -m build --wheel -o dist

      - name: Build sdist
        run: |
          python -m build --sdist -o dist

      - name: Upload built artifacts
        uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist/*

  release:
    name: Create GitHub Release
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download built artifacts
        uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist

      - name: Create GitHub Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          VERSION="${{ github.ref_name }}"
          VERSION="${VERSION#v}"  # Strip leading 'v'
          cat << EOF > release_body.md
          ## 📦 Installation

          ### Quick Install (Recommended)
          \`\`\`bash
          pip install torchlingo==${VERSION}
          \`\`\`

          ### Google Colab
          \`\`\`python
          %pip install torchlingo==${VERSION}
          \`\`\`

          ## 📚 Resources

          - 📖 [Documentation](https://byu-matrix-lab.github.io/torchlingo/)
          - 🚀 [Quick Start Guide](https://byu-matrix-lab.github.io/torchlingo/getting-started/quickstart/)
          - 📓 [Tutorials](https://byu-matrix-lab.github.io/torchlingo/tutorials/)

          ---
          EOF

          gh release create ${{ github.ref_name }} \
            --title "Release ${{ github.ref_name }}" \
            --notes-file release_body.md \
            --generate-notes \
            dist/*

  publish:
    name: Publish to PyPI
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: pyproject.toml

      - name: Download built artifacts
        uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist

      - name: Install packaging tools
        run: |
          python -m pip install --upgrade pip
          pip install twine

      - name: Publish to PyPI
        env:
          TWINE_USERNAME: __token__
          TWINE_PASSWORD: ${{ secrets.PYPI_API_TOKEN }}
        run: |
          python -m twine upload --non-interactive dist/*